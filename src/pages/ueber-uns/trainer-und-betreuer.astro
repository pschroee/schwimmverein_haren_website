---
import Layout from "../../layouts/Layout.astro"
import PeopleCard from "../../components/PeopleCard.astro"
import trainerundbetreuerJson from "../../data/ueberuns-trainerundbetreuer.json"

const breadcrumbs = [
  { title: "Über uns", href: "/ueber-uns/" },
  { title: "Trainer & Betreuer" },
]

const persons = trainerundbetreuerJson.trainerundbetreuer
---

<Layout title="Trainer & Betreuer" breadcrumb={breadcrumbs}>
  <div class="max-w-screen-lg mx-auto">
    <h1 class="col-span-2 mb-6 text-2xl sm:text-4xl font-bold text-gray-800">
      Trainer & Betreuer
    </h1>
    <ul class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
      {
        persons.map((person) => (
          <PeopleCard
            name={person.name}
            function={person.function}
            email={person.email}
            phone={person.phone}
            image={person.image}
          />
        ))
      }
    </ul>
  </div>
</Layout>
